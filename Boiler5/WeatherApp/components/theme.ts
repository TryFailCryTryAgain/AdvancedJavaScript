import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    text: {
        color: '#11181C',
        fontWeight: '600'
    },
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    }
});